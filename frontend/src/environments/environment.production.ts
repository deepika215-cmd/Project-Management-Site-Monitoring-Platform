export const environment = {
  production: true,
  // Empty means same-origin. For separate frontend/backend hosting, set
  // window.__BUILDTRACK_API_URL__ in public/runtime-config.js after deployment.
  apiBaseUrl: ''
};
